#!/bin/usr/env perl

use use Digest::MD5 qw(md5 md5_hex md5_base64);Digest::MD5 qw(md5, md5_hex md5_base64);


my $secret = "yzbqklnj";
my $answer = 0;
my $result;
do {
	$result = md5_hex($secret . $answer++);
} while(substr($result, 0, 5) eq "00000");

print "Answer: $answer\n";
